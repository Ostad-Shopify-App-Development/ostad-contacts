<div id="shopify-app-init"
     data-api-key="{{ $apiKey }}"
     data-shop-origin="{{ $shopOrigin }}"
     data-host="{{ $host }}"
     data-force-iframe="{{ $forceRedirect ? 'true' : 'false' }}"
     data-load-path="{{ $loadPath }}"
     data-jwt-expire-at="{{ $expireAt }}"
></div>
